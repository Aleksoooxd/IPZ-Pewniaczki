name: Daily Python Script Execution

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  run-python-script:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python environment
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Check if requirements.txt exists
        run: |
          if [ ! -f requirements.txt ]; then echo \"requirements.txt not found\"; exit 1; fi
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Debug Python script
        run: |
          echo \"Running src/main.py...\"
          python src/main.py || { echo \"Script failed with exit code $?\"; exit 1; }
